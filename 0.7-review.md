# MikPlay v0.7 Code Review & Optimizations

## 🎨 DOS VGA Color Codes (Your Question)

### Standard 16-Color Palette
```c
// Foreground/Background colors (0-15)
0x00 = Black
0x01 = Blue (dark blue)
0x02 = Green (dark green)      // ✓ DARK GREEN
0x03 = Cyan (dark cyan)
0x04 = Red (dark red)           // ✓ DARK RED  
0x05 = Magenta (dark magenta)
0x06 = Brown (dark yellow)      // ✓ DARK YELLOW
0x07 = Light Gray               // ✓ DARK GRAY (actually light gray)
0x08 = Dark Gray                // True dark gray
0x09 = Light Blue
0x0A = Light Green
0x0B = Light Cyan
0x0C = Light Red
0x0D = Light Magenta (Pink)
0x0E = Yellow (bright yellow)
0x0F = White

// Color attribute format: (background << 4) | foreground
#define COL_DKGREEN_BLACK  0x02  // Dark green on black
#define COL_DKRED_BLACK    0x04  // Dark red on black
#define COL_BROWN_BLACK    0x06  // Brown/dark yellow on black
#define COL_DKGRAY_BLACK   0x08  // Dark gray on black
#define COL_LTGRAY_BLACK   0x07  // Light gray on black
```

---

## 🔴 Critical Bugs Fixed

### ✅ 1. VU Meter Bug RESOLVED (Line 308)
**Status**: FIXED! 
```c
// OLD CODE (BUG):
if (vol > max_volume) max_volume = vol * 4;  // Exponential growth!

// NEW CODE (CORRECT):
if (vol > max_volume) max_volume = vol;
```
**Result**: VU meter now works correctly! No more permanent maxed-out display.

---

### ✅ 2. Buffer Overflow Fixed (Line 278)
**Status**: FIXED!
```c
// OLD CODE (BUG):
Player_QueryVoices((num_voices > MAX_VOICES_CAP) ? num_voices : MAX_VOICES_CAP, voice_info);

// NEW CODE (CORRECT):
Player_QueryVoices((num_voices > MAX_VOICES_CAP) ? MAX_VOICES_CAP : num_voices, voice_info);
```
**Result**: No more potential buffer overflow when num_voices > 128.

---

### ⚠️ 3. Array Index Bug (Lines 310-311) - STILL PRESENT
```c
// CURRENT CODE (PROBLEMATIC):
for (i = 0; i < num_voices; i++)
{
    // ... sample/instrument tracking in inner loop ...
    
    if (sample_active[i]) active_samples++;      // BUG: 'i' is voice index
    if (instrument_active[i]) active_instruments++; // BUG: 'i' is voice index
}
```

**Problem**: The loop variable `i` ranges from 0 to `num_voices` (up to 128), but it's used to index `sample_active[]` and `instrument_active[]` arrays which are sized for samples/instruments (could be 20-30). This will access garbage data beyond array bounds.

**Fix**: Remove these two lines completely - the counters are already incremented correctly in the inner loop:
```c
for (i = 0; i < num_voices; i++)
{
    if (Voice_Stopped(i)) continue;
    if (i < MAX_IND) active_channel_bar[i] = CH_IND;
    
    vol = Voice_GetVolume(i);
    active_channels++;
    
    if (voice_info[i].s != NULL)
    {
        int sample_idx = (int)(voice_info[i].s - module->samples);
        if (sample_idx >= 0 && sample_idx < module->numsmp && sample_idx < MAX_IND)
        {
            active_sample_bar[sample_idx] = CH_IND;
            sample_active[sample_idx] = 1;
            active_samples++;  // ← Already counted here!
        }
    }
    
    if (voice_info[i].i != NULL)
    {
        int inst_idx = (int)(voice_info[i].i - module->instruments);
        if (inst_idx >= 0 && inst_idx < module->numins && inst_idx < MAX_IND)
        {
            active_instrument_bar[inst_idx] = CH_IND;
            instrument_active[inst_idx] = 1;
            active_instruments++;  // ← Already counted here!
        }
    }
    
    if (vol > max_volume) max_volume = vol;
    
    // DELETE THESE TWO LINES:
    // if (sample_active[i]) active_samples++;
    // if (instrument_active[i]) active_instruments++;
}
```

---

## ⚡ Performance Optimizations

### 1. Reduce VRAM Writes (Save 30-50% CPU)
**Current**: Redraws ~100 characters every frame (10 Hz) even when unchanged.

**Optimization**: Track previous state and only update changed values:
```c
void draw_playback_panel(const MODULE *module, int volume, int update)
{
    // Add static variables to track previous state
    static int prev_sngpos = -1;
    static int prev_patpos = -1;
    static int prev_volume = -1;
    static int prev_active_inst = -1;
    static int prev_active_chan = -1;
    static int prev_active_samp = -1;
    static int prev_vu_level = -1;
    static int prev_progress_level = -1;
    static int prev_row_level = -1;
    
    // Calculate current values...
    
    // Only update if changed
    if (module->sngpos != prev_sngpos || module->numpos != prev_numpos) {
        strcpy(buf, "Patt: "); itoa3(buf + 6, module->sngpos); 
        buf[9] = '/'; itoa3(buf + 10, module->numpos - 1); 
        buf[13] = '\0';
        write_string_attr(43, 4, buf, COL_BLACK_CYAN);
        prev_sngpos = module->sngpos;
    }
    
    if (module->patpos != prev_patpos) {
        strcpy(buf, "Row : "); itoa3(buf + 6, module->patpos);
        write_string_attr(43, 5, buf, COL_BLACK_CYAN);
        prev_patpos = module->patpos;
    }
    
    // Repeat for other values...
}
```

**Expected Gain**: 30-50% reduction in UI update overhead.

---

### 2. Static Voice Info Buffer (Save Stack Space)
**Current**: 20KB+ on stack every call (Line 274)
```c
static VOICEINFO voice_info[MAX_VOICES_CAP];
```

**Optimization**: Already done! ✓

---

### 3. Optimize itoa Functions
**Current** (Lines 127-139): Good implementation, but can be slightly faster:
```c
// Add lookup table at top of file
static const char digits[] = "0123456789";

static inline void itoa2_fast(char *buf, int val)
{
    buf[0] = digits[val / 10];
    buf[1] = digits[val % 10];
    buf[2] = '\0';
}

static inline void itoa3_fast(char *buf, int val)
{
    buf[0] = digits[val / 100];
    buf[1] = digits[(val / 10) % 10];
    buf[2] = digits[val % 10];
    buf[3] = '\0';
}
```

**Expected Gain**: Marginal (5-10% faster on these specific functions).

---

### 4. Reduce Player_QueryVoices() Overhead
**Current**: Called every frame for all voices

**Optimization**: Only query voices when needed, or cache results:
```c
// Option A: Reduce query frequency for less critical data
static int query_counter = 0;
if (++query_counter >= 3) {  // Only query every 3rd frame
    Player_QueryVoices((num_voices > MAX_VOICES_CAP) ? MAX_VOICES_CAP : num_voices, voice_info);
    query_counter = 0;
}
```

**Expected Gain**: 10-15% CPU reduction on update calls.

---

## 🛠️ Code Quality Improvements

### 1. Define Magic Numbers
```c
// Add to defines section
#define UPDATE_RATE_NORMAL    10    // Updates per second
#define UPDATE_RATE_386       4     // Updates per second (slow mode)
#define VU_SCALE_FACTOR       1     // VU meter scaling (removed * 4)
#define VOLUME_STEP           8     // Volume change increment
#define MAX_VOLUME_LEVEL      128   // Maximum volume
#define MIN_VOLUME_LEVEL      0     // Minimum volume
#define PITCH_MIN_PERCENT    -50    // Minimum pitch shift
#define PITCH_MAX_PERCENT     100   // Maximum pitch shift
```

---

### 2. Const Correctness
Already applied! ✓
```c
void draw_info_panel(const MODULE *module)
void draw_playback_panel(const MODULE *module, int volume, int update)
```

---

### 3. Better Bounds Checking
```c
// Add macro for safe array access
#define SAFE_ARRAY_ACCESS(arr, idx, max) \
    ((idx) >= 0 && (idx) < (max))

// Use in code:
if (SAFE_ARRAY_ACCESS(sample_active, sample_idx, module->numsmp) && 
    sample_idx < MAX_IND)
{
    active_sample_bar[sample_idx] = CH_IND;
    sample_active[sample_idx] = 1;
    active_samples++;
}
```

---

## 📊 VU Meter Color Scheme Suggestion

For better visual feedback, consider this gradient (Line 349):
```c
// VU meter with improved color gradient
int ccol;
if (i < 3)       ccol = 0x08;      // Dark gray (very quiet)
else if (i < 8)  ccol = 0x02;      // Dark green (quiet)
else if (i < 13) ccol = 0x0A;      // Light green (normal)
else if (i < 17) ccol = 0x0E;      // Yellow (loud)
else if (i < 19) ccol = 0x06;      // Brown/dark yellow (very loud)
else             ccol = 0x04;      // Dark red (clipping!)

write_char_attr(57 + i, 10, i < vu_level ? CH_BLOCK : CH_HLINE, 
                i < vu_level ? ccol : 0x08);
```

---

## 🎯 Summary of Required Fixes

### Critical (Fix Immediately):
1. ✅ VU Meter multiplication bug - **FIXED**
2. ✅ Buffer overflow in Player_QueryVoices - **FIXED**  
3. ❌ Array indexing bug (lines 310-311) - **NEEDS FIX**

### Recommended (Performance):
1. Selective VRAM redraw (30-50% improvement)
2. Reduce voice query frequency (10-15% improvement)
3. Cache unchanged UI elements

### Optional (Code Quality):
1. Add magic number defines
2. Improve bounds checking macros
3. Better error handling

---

## 📈 Expected Performance Impact

| Optimization | CPU Savings | Implementation Effort |
|--------------|-------------|----------------------|
| Fix array indexing bug | Safety fix | 2 minutes |
| Selective VRAM updates | 30-50% | 30 minutes |
| Reduce voice queries | 10-15% | 10 minutes |
| Total estimated gain | **35-60%** | **42 minutes** |

---

## 🎨 Color Reference for Your UI

```c
// Suggested color scheme additions
#define COL_DKGREEN_BLACK  0x02  // For VU meter (quiet)
#define COL_GREEN_BLACK    0x0A  // For VU meter (normal)
#define COL_YELLOW_BLACK   0x0E  // For VU meter (loud)
#define COL_BROWN_BLACK    0x06  // For VU meter (very loud)
#define COL_DKRED_BLACK    0x04  // For VU meter (clip)
#define COL_DKGRAY_BLACK   0x08  // For inactive indicators
#define COL_LTGRAY_BLACK   0x07  // For inactive text
```

Good luck with the optimizations! 🚀